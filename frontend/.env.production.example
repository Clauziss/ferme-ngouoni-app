# En local (npm run dev), le frontend passe par le proxy Vite vers localhost:4000,
# pas besoin de ce fichier. Pour le déploiement en ligne uniquement :
# copie ce fichier en .env.production et mets l'URL de ton backend déployé.
VITE_API_URL=https://ton-backend.onrender.com/api
