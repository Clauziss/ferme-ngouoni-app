PORT=4000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ferme_avicole
DB_USER=postgres
DB_PASSWORD=postgres
JWT_SECRET=change_moi_en_production
JWT_EXPIRES_IN=7d
SEUIL_REFORME_PONTE=0.5

# Notifications par e-mail (facultatif — si vide, les e-mails sont juste ignorés)
# Exemple avec Gmail : SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_USER=ton_email,
# SMTP_PASS=un "mot de passe d'application" Google (pas ton mot de passe normal)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Ferme de Ngouoni <notifications@ferme-ngouoni.local>"
